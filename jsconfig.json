{
  "compilerOptions": {
    "target": "es2015"
  },
  "exclude": ["node_modules"],
  "path-intellisense.mappings": {
    "/": "${workspaceRoot}",
    "src": "${workspaceRoot}/src",
    "stories": "${workspaceRoot}/stories"
  }
}
